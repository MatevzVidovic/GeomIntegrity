
# Task


look at what we are doing in AgentDocs/

Read GENERAL.md in StarterPrompts

  i need you to make the tests far more comprehensive then they are now

  I see we currently cover basically only a few cases

  I want to comprehensively cover all cases

  If things fail, go fix them

  I am giving you access to the db in db_config so you can run these scripts and run the tests

  DONT CHANGE ANYTHING IN THE DB THAT TOUCHES ANTRHING EXCEPT md_topoloske_kontrole_obm and mmd_topoloske_kontrole_hierarhija
  (Except for how we do set_reduceprecision in obms, but that is it)

  You can also do ROLLBACKABLE changes as we do in the test, but make sure they are rollbackable, and make sure they are rolled back.


## Refined Prompt (Agent)

Create a much more comprehensive, rollback-safe test suite that validates the full OBM topology and hierarchy validation system end-to-end. The new tests must:
- Cover all problem types and key trigger paths (insert/update/delete) for both topology and hierarchy validations.
- Use strict assertions (fail fast), not only printed output.
- Run only inside transactions and always rollback.
- Avoid persistent DB changes; any setup data must be rollbacked.
- Stay within the requested DB-scope constraints (no permanent DB changes outside rollbackable test activity).
- Provide a single `make test-agent` command that runs all agent-created tests in `AgentTests/`.

## Plan (Agent)

1. Audit existing SQL functions/triggers and enumerate all behavioral branches and problem types to test.
2. Create `AgentTests/` with:
   - a comprehensive full-validation suite (batch functions),
   - a comprehensive incremental-trigger suite (topology + hierarchy),
   - strict assertion helpers.
3. Add `Makefile` target `test-agent` to run all `AgentTests` scripts against DB credentials from `db_config/config.py`.
4. Execute tests thoroughly, fix SQL function/trigger issues found by tests, and rerun until green.
5. Update stage checklist below as work progresses.

## Ambiguities / Questions (Agent)

No blocking ambiguities found. Proceeding with safe rollback-only tests and direct fixes for discovered failures.

## Stages (Agent)

- [x] Stage 1: Define comprehensive coverage matrix and test design
- [x] Stage 2: Implement `AgentTests` rollback-safe SQL test suites
- [x] Stage 3: Add `make test-agent` runner
- [x] Stage 4: Execute tests, fix failures, and re-run until all pass

## Execution Notes (Agent)

- Implemented comprehensive rollback-safe test suites under `AgentTests/`:
  - `01_full_validations.sql`
  - `02_topology_trigger_incremental.sql`
  - `03_hierarchy_trigger_incremental.sql`
- Added `AgentTests/run_agent_tests.sh` and `Makefile` target `test-agent`.
- Fixed discovered SQL defects:
  - `Main/2_0_fn_obm_geom_check_all.sql`: corrected `validate_all(uuid)` empty-version return row shape.
  - `Main/3_1_trg_hierarchy_triggers.sql`: improved `validate_obmxcona_incremental()` to revalidate old and new model versions on UPDATE.
- Final verification:
  - `make test-agent` succeeded (`EXIT_CODE:0`).
